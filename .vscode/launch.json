{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/zig-out/bin/${",
            "args": [],
            "env": {},
            "cwd": "${workspaceRoot}"
        }
    ]
}